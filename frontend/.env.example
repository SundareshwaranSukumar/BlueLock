# Copy to .env.local for local development

VITE_USE_BACKEND=true
BACKEND_URL=http://127.0.0.1:8000

# Optional: call Cloud Run directly from the browser
# VITE_API_BASE_URL=https://your-service-uc.a.run.app

# WebSocket telemetry (defaults to proxied /api/v1/stadium/live-stream when USE_BACKEND=true)
# VITE_TELEMETRY_WS_URL=ws://127.0.0.1:8000/api/v1/stadium/live-stream
