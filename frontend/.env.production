# Production build for Firebase Hosting (same-origin /api and WebSocket via Hosting rewrites).
# Do not set VITE_API_BASE_URL or VITE_TELEMETRY_WS_URL — the client uses window.location.
VITE_USE_BACKEND=true
